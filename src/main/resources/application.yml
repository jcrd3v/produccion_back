spring:
  application:
    name: controlproduccionback
  datasource:
    url: jdbc:postgresql://localhost:5432/control_produccion
    username: jcrdev
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true